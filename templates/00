<!-- {% extends "base.html" %}
{% block content %}
<div class="container mt-4">
  <div class="text-center mt-4 mb-4">
        <a href="{{ url_for('user_detail', user_id=user_id) }}" class="btn btn-secondary btn-lg">
            Return to User
        </a>
  </div>
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Performance Logs</h2>
    <a href="{{ url_for('add_performance', user_id=user_id) }}" class="btn btn-primary">Add Log</a>
  </div>

  <table class="table table-striped table-sm align-middle">
    <thead>
      <tr>
        <th>Log ID</th>
        <th>Date</th>
        <th>Duration (min)</th>
        <th>Exercise</th>
        <th>Sets</th>
        <th>Reps</th>
        <th>Weight (kg)</th>
        <th>Volume</th>
        <th>Category</th>
        <th>Equipment</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
    {% for log in logs %}
      <tr>
        <td>{{ log.LogID }}</td>
        <td>{{ log.Date }}</td>
        <td>{{ log.Duration }}</td>
        <td>{{ log.Exercise }}</td>
        <td>{{ log.Sets }}</td>
        <td>{{ log.Reps }}</td>
        <td>{{ log.Weight }}</td>
        <td>{{ log.Volume }}</td>
        <td>{{ log.Category }}</td>
        <td>{{ log.Equipment }}</td>
        <td>
          <a href="{{ url_for('edit_performance', log_id=log.LogID) }}" class="btn btn-sm btn-outline-secondary">Edit</a>
          <form action="{{ url_for('delete_performance', log_id=log.LogID) }}" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
          </form>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

    
</div>
{% endblock %}
 -->
